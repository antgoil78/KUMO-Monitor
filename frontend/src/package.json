{
  "name": "kumo-monitor-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal frontend built into ./build for Flask static serving.",
  "scripts": {
    "build": "node build.js",
    "start": "node dev-server.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
